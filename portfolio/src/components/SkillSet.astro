---
const GROUPS = [
  {
    title: 'Interfaces',
    description: 'Interfaces claras con buena jerarquía, estructura y atención al detalle.',
    items: ['React', 'TypeScript', 'JavaScript', 'HTML', 'CSS', 'Tailwind', 'Redux']
  },
  {
    title: 'Sistemas',
    description: 'Lógica de aplicación, APIs, servicios y diseño de componentes de backend.',
    items: ['Node.js', 'Express', 'Python', 'Flask', 'Java', 'REST API']
  },
  {
    title: 'Datos e IA',
    description: 'Modelado de datos, persistencia y base técnica sobre la que explorar IA aplicada.',
    items: ['PostgreSQL', 'MySQL', 'MongoDB']
  },
  {
    title: 'Infra y tooling',
    description: 'Entorno de trabajo, testing, despliegue y herramientas para resolver problemas con criterio.',
    items: ['Docker', 'Jest', 'GitHub', 'Linux', 'AWS', 'Postman']
  }
]
---

<div class="stack-flow">
  {
    GROUPS.map((group) => (
      <article class="stack-item">
        <p class="stack-label">{group.title}</p>
        <p class="stack-description">{group.description}</p>
        <p class="stack-items">{group.items.join(' / ')}</p>
      </article>
    ))
  }
</div>

<style>
  .stack-flow {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 22px 36px;
  }

  .stack-item {
    display: grid;
    gap: 6px;
  }

  .stack-label {
    font-family:
      ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono',
      'Courier New', monospace;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .stack-description {
    max-width: none;
    font-size: 15.5px;
    line-height: 1.66;
    color: var(--muted-ink);
  }

  .stack-items {
    max-width: none;
    font-size: 15px;
    line-height: 1.6;
    color: var(--ink);
  }

  @media (max-width: 900px) {
    .stack-flow {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }
</style>
